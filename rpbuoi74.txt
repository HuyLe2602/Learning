1.Push là mô hình mà Server chủ động gửi dữ liệu đến Client khi có thay đổi

2.Client tạo kết nối đến server -> server giữ kết nối đó mở -> Khi có dữ lieu server sẽ gửi ngay đến client mà k chờ hỏi lại

3.Majority Element là bài toán tỉ lệ chiếm hơn 1 nửa triệt tiêu nhau bằng cặp cho đến khi 1 bên hết, bên còn lại lại là chiếm hơn 1 nửa dùng vòng lặp for và if else

Code:

class Solution {
    public int majorityElement(int[] nums) {
        
        int candidate = 0;
        int count = 0;

        for (int num : nums) {

            if (count == 0) {
                candidate = num;
            }

            if (num == candidate) {
                count++;
            } else {
                count--;
            }
        }

        return candidate;
    }
}

4.Single Number là in ra phần tử chỉ xuất hiện đúng 1 lần ta dùng vòng lặp for 

Code:
	class Solution {
    public int singleNumber(int[] nums) {
         int result = 0;

        for (int num : nums) {
            result ^= num;
        }

        return result;
    }
    }
